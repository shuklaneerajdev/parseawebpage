<%= form_for(urlexplorer) do |f| %>
  <% if urlexplorer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(urlexplorer.errors.count, "error") %> prohibited this urlexplorer from being saved:</h2>

      <ul>
      <% urlexplorer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :url, placeholder:'Enter the url to parse', autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Parse", class: "btn btn-primary btn-lg"%>
  </div>
<% end %>
